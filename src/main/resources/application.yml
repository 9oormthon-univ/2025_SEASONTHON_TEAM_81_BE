spring:
  profiles:
    active: local
    group:
      local: "local, datasource"
      dev: "dev, datasource"
      prod: "prod, datasource"
    include:
      - docs
      - redis
      - security
  jpa:
    open-in-view: false